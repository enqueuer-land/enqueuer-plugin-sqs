{
  "name": "enqueuer-plugin-sqs",
  "version": "0.0.1",
  "description": "Enqueuer sqs plugin",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "all": "npm run clean && npm run lint && npm run build",
    "build": "tsc",
    "clean": "rm -rf dist",
    "lint": "node_modules/.bin/tslint --project tsconfig.json",
    "prepublishOnly": "npm run all"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/lopidio/enqueuer-plugin-sqs.git"
  },
  "keywords": [
    "enqueuer",
    "nqr",
    "sqs",
    "plugin"
  ],
  "author": "Virgs",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/lopidio/enqueuer-plugin-sqs/issues"
  },
  "homepage": "https://github.com/lopidio/enqueuer-plugin-sqs#readme",
  "devDependencies": {
    "@types/node": "^11.9.4",
    "ts-node": "^8.0.2",
    "tslint": "^5.11.0",
    "typescript": "^3.0.1"
  },
  "dependencies": {
    "enqueuer-plugins-template": "^1.0.4",
    "aws-sdk": "^2.386.0"
  }
}
